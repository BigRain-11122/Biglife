# RUMOR-CHAIN 流言链契约 v1.0（判据先行·2026-09-24 立契）

> T-20260924-16 **b 分单**。法源=R-20260924-silicon-aliveness §四增量 2（cph4/research/ 只读）+集团 P-75 行（09-24 ~21:45 明文点名「流言链/情绪导演=本件消费面向」）。
> 定位：事件经居民 A→B 确定性跳数传播（带冷却+失真）——用户从他人之口听见城市=社会性在场证明。
> 引擎=`Tools/rumor_chain.py`（下轮落件·零 LLM 零 API·纯确定性）；本契约为引擎验收判据，全过才关分单。

## 一 接口（按需调用面·非 R3 再生面·不入 census/export 正典面）

```
python -X utf8 Tools/rumor_chain.py --event-id ID --event-text "文本" --seed C-xxxxx \
  [--hops 3] [--cool-min 60] [--at "YYYY-MM-DD HH:MM"] [--out 路径] [--qc]
```

输出：JSON 一对象=`{event_id, seed, hops, cool_min, at, engine_v, hops:[{hop, from, to, ts, text, op}]}`；`--out` 落文件，缺省 stdout。

## 二 验收判据（门禁）

1. **确定性**：同 (event, seed, 参数) 双跑输出逐字节一致——选跳/延迟/失真全 md5 派生（`python hash()` 进程随机化禁用=behavior.py 同律）。
2. **跳数参数化**：`--hops` 有效域 1..6，默认 3；产出跳数=参数值。
3. **冷却参数化**：`--cool-min` 默认 60；相邻跳 ts 间隔 ≥ cool-min（确定性延迟=cool-min + md5%cool-min，上界 2×cool-min——事件不瞬移）；`cool-min=0` 合法（不冷却档·FP 护栏）。
4. **失真有界零编造**：每跳至多 1 次闭集变换 `{截尾(末分句)、数字模糊(阿拉伯数字→「几」)、垫话前缀(「听说」「好像」二选一)、无操作}`；输出字符集 ⊆ 原事件文本 ∪ 垫话闭集——零新增事实。垫话系流言面角色真实转述语义，与年轮面「天气消息源归属禁令」（缺口#8 族）不冲突：本面禁含天气/天体/时段词族（机审门触发词族规避=R40 纪律）。
5. **选人词域**：同城区优先（seed 城区池，池尽则全城池）；链内居民去重（一人一链一次=链内冷却）+时间冷却（判据 3）双面。
6. **--qc 自检**：判据 1-5 断言全过（双跑逐字节一致+跳数/冷却边界+失真闭集+链内零重复+cool-min=0 护栏）才 PASS。

## 三 消费点与红线

- 消费方=FluxVerse M2 互聊机制升级（CityWatch/参观端「居民口中听见城市」）+年轮喂入面候选（流言=事件二级传播源，消费时标注 hop 谱系）。
- 红线：FluxVerse 事件流只读零写回；T2 正典登记（CODEX §十二）随引擎落件轮一并办（契约本身零接口变更零登记义务）。

## 四 年轮批消费面（断点①·T-20260926-10 分步①·2026-09-26 R335 增补·法源=集团决策 D-20260926-07+R-20260925-alive-city 分步④ R281 四断点实证）

接线=`Tools/evolve_citizen.py rumor_line_for(cid, sig)`→batch prompt 候选事实块（meet/reflect 面零触碰）：

1. **选件律**：候选池=喂入事件流中双滤后件——①R40 触发词滤（天气/天体/时段/场次/放学全族=判据 4 纪律对流言面整体生效）②开发标识滤（`round 轮次号|R\d{2,} 档号|X\d{3,} 台账号|#\d 单号`=prompt 明禁入环的开发流水件永不作为逐字引用候选）；md5(cid, event) 榜序确定性选一件，事本体=事件串尾字段（type/zone/actor 为元数据）。
2. **谱系律**：seed=到期居民本人（传播起点·md5 派生 hops=2·cool_min=0=prompt 非时间线面）；prompt 块标注双跳谱系「你→A→B」（A/B=同城区优先链·真实在册 id·可提可不提）。
3. **频率律**：1/3 md5 hash 门（`m_int(cid, date, "rumor") % 3`·随 (id,日期) 恒定）——批内约 1/3 居民见到候选块，候选块非强制素材（「可提可不提」明示）。
4. **引用律**：年轮提及传闻只许转述口吻+逐字引用闭集失真后文本（字符集⊆原文∪垫话闭集=零新增事实），禁加细节；荣誉席 C-00001~03 永不自动入链（引擎 RESERVED 同律）。
5. **判据（验收实录 R335）**：纯读面零新系统零 LLM 新增面·同 (feed,now,cid) 双跑逐字节一致·**冻结快照等价=未命中位（hash miss/无净件/链护栏）prompt 与接线前逐字节一致（旧版模块对照实证）**·谱系可重 derive（lineage ids==build_chain tos）·gate v0.31 零触碰（传闻文本⊆喂入事件子串=既有事件子串豁免锚面不灭）·py_compile 过。
6. **观察窗（下批起）**：实弹验证窗=后续批观察传闻块引用形态（转述口吻/逐字/零引用皆合法）；若现「给传闻添加细节」型逃逸→按缺口程序复审（R287 范式 FP 普查先行）。
